@model IEnumerable<u19074362_HW5_2._5.Models.returnbook>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.book)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.returndate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.elap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.book)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.returndate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.elap)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fine)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
